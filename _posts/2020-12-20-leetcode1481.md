---
layout:     post
title:      1481. Least Number of Unique Integers after K Removals
date:       2020-12-20
author:     skymelody
header-img: img/the-first.png
catalog: false 
tags:
    - leetcode

---

Given an array of integers `arr` and an integer `k`. Find the *least number of unique integers* after removing **exactly** `k` elements**.**



Examples

```
Input: arr = [5,5,4], k = 1
Output: 1
Explanation: Remove the single 4, only 5 is left.

Input: arr = [5,5,4], k = 1
Output: 1
Explanation: Remove the single 4, only 5 is left.
```



思路：

unordered_map + priority_queue

先用unordered_map统计数组中每个unique integer的个数， 再以unique integer的个数建立一个小顶堆。 

```cpp
class Solution {
public:
    int findLeastNumOfUniqueInts(vector<int>& arr, int k) {
        unordered_map<int, int> cnt;
        for (int val : arr) {
            ++cnt[val];
        }
        
        auto comp = [](const pair<int, int>& pa, const pair<int, int>& pb) {
            return pa.second > pb.second;
        };
        
        priority_queue<pair<int, int>, vector<pair<int, int>>, decltype(comp)> pq(comp);
        
        for (const auto& p : cnt) {
            pq.push(p);
        }
        
        while ( k > 0 ) {
            auto cur = pq.top();
            if (cur.second - k <= 0) {
                k -= cur.second;
                pq.pop();
            }
            else {
                break;
            }
        }
        
        return pq.size();
    }
};
```



